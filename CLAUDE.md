# Guidelines

## Knowledge
トップレベルのディレクトリの切り方は、Go言語の標準ディレクトリ構造を参考に必要なディレクトリのみを作成する。  
またトップレベルにREADME.mdを配置し、プロジェクトの概要を記載する。利用しているGoライブラリについてはgo.mod。  
その他、プロジェクトに関連する情報はdocsディレクトリにまとめる。  

## Development
Goの開発に必要なコマンドはMakefileにまとめる。  

特に開発時に利用するのは以下のコマンド  
- make migration  
  マイグレーションファイルを作成する  
- make lint  
  静的解析を実行して構文の問題を検出する。ビルドの確認にも利用する。  
- make singlet file=<file or directory>  
  fileやdirectoryを指定して単体テストを実行する  
- make runt  
  E2Eテストを実行する  

開発の際は、ファイルを一つ一つ作成し、まとめて作成しない。
パッケージの開発が完了したら、テストコードを作成し、CIでテストが通ることを確認する。

## Prohibitions
- 秘密情報を追加しない。生成コードにAPIキーや暗号鍵を埋め込まない。  
- コードの修正時にsed、perl、awk等のテキスト処理コマンドやスクリプトを使用しない。必ずeditツールを使用する。
- gitの操作は全面的に禁止する。

